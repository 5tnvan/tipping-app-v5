{
  "name": "wildpay-neo-testnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wildpay-neo-testnet",
    "create-local": "graph create --node http://localhost:8020/ wildpay-neo-testnet",
    "remove-local": "graph remove --node http://localhost:8020/ wildpay-neo-testnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wildpay-neo-testnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.5",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
